

#include "Canvas.h"
#include <map>


static std::map<, >

long int simple_hash(std::string type)
{
    long int value

    for(char a : type)
    {

    }
}

int LoadConfigFile(std::string filename)
{
    std::fstream config(filename);
    config >>filename;



}
